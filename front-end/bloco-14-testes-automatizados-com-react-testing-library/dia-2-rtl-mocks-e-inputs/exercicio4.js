const caixaAlta = (string) => string.toUpperCase();


const firstWord = (string) => string.charAt();


const concatena = (string, word) => `${string} ${word}`;

module.exports = {
    caixaAlta,
    firstWord,
    concatena
};
